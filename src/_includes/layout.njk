<!DOCTYPE html>
<html lang="en" class="scroll-smooth">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/assets/main.bundle.css">
  <script src="/assets/main.bundle.js" defer></script>
  <title>{{ title }}</title>
</head>

<body class="{{ 'debug-screens' if env.devMode }} bg-white dark:bg-neutral-800 flex flex-col justify-between min-h-screen">
  {% include 'header.njk' %}
  {% block body %}
    {{ content | safe }}
  {% endblock %}
  {% include 'footer.njk' %}
</body>
</html>
